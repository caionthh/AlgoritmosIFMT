#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
struct servicos
{
	int cod;
	char desc[30];
};

struct empresa
{
	int num;
	int valor;
	int cod_serv;
	int cod_cliente
};
int i, j, op, codigo_cliente, valor_serv, num_serv, valida;
int valor_inicial, valor_final, k;
struct servicos tipos;
struct empresa serv[30][3];

int main(int argc, char *argv[]) {
	
	do
	{
		printf("1 - Cadastrar tipos de serviços\n");
		printf("2 - Cadastrar serviços prestados\n");
		printf("3 - Mostrar os serviços prestados em um determinado dia\n");
		printf("4 - Mostrar os serviços prestados dentro de um intervalo de valor\n");
		printf("5 - Mostrar um relatório geral, separado por dia\n");
		printf("6 - Sair\n");
		printf("Digite sua opção\n");
		scanf("%d", &op);
		
		switch(op)
		{
			case 1:
			
			printf("Digite o código do serviço a ser cadastrado:");
			scanf("%d", &codigo_serv);
			achou=0;
			
			for(i=0 ; i<4 ; i++)
			{
				if(tipos[i].cod=0)
				achou=i;
			}
			
			if(achou = 0)
			printf("Cadastro de serviços lotado\n");
			else
			{
				for(i=0; i<4; i++)
				{
					if(tipos[i].cod=cod_serv)
					achou=0;
				}
				if(achou = 0)
				printf("Já existe um tipo de serviço cadastrado com este codigo\n");
				
				else
				{
					printf("Descrição:");
					scanf("%s", &desc_serv);
					tipos[i].cod=codigo_serv;
					tipos[i].cod=desc_serv;
					printf("Cadastro com sucesso\n");
				}
				
			}
			break;
			
			case 2:
			
			printf("Digite o dia:");
			scanf("%d", &dia);
			
			achou=0;
			
			for(j=0 ; j<3; j++)
			{
				if()
			}
		}
	}
	return 0;
}